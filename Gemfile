source "https://rubygems.org"

# Matches the gem set GitHub Pages runs, so local previews stay in sync.
gem "github-pages", group: :jekyll_plugins

# Required for Ruby 3.0+ (WEBrick is no longer bundled).
gem "webrick", "~> 1.8"
